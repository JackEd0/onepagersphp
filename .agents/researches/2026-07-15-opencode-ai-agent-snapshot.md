# Research: opencode AI agent snapshot folder storage

## Metadata

- **Date**: 2026-07-15 20:11
- **Original query**: opencode AI agent snapshot folder storage
- **Refined query**: opencode ai agent snapshot folder storage
- **Engines used**: tavily, exa
- **Total results**: 10
- **Per-engine limits**: tavily=5, exa=5
- **Response time**: 4000 ms

## Tavily

**Answer:** The OpenCode AI agent's snapshot folder can grow large, potentially causing disk space issues. Deleting it is generally not recommended, but if necessary, proceed with caution. Always back up important data before deletion.

> *Unverified — cross-check for factual, pricing, or CLI-syntax queries.*

### [1] HUGE snapshot folder · Issue #6845 · anomalyco/opencode · GitHub

- **URL**: https://github.com/anomalyco/opencode/issues/6845

# Search code, repositories, users, issues, pull requests... You signed in with another tab or window. You switched accounts on another tab or window. * Notifications  You must be signed in to change notification settings. bugSomething isn't workingSomething isn't workingnixperfIndicates a performance issue or need for optimizationIndicates a performance issue or need for optimization. I was setting up a nixos vm and found that I had way less space on my disk than usual but I didn't download anything very big for a while. So I launched filelight to see what was going on and turns out the ~/.local/share/opencode folder was 169gb!!!!! It seems to be stuff in `~/.local/share/opencode/snapshot/global/objects/pack`. Is it a known issue, I didn't really find anything ? I update opencode almost always the day or next day when there is an update, so maybe it was a bug in earlier versions ? ### Screenshot and/or share link. No fields configured for issues without a type. No branches or pull requests.

### [2] opencode-ai

- **URL**: https://classic.yarnpkg.com/en/package/opencode-ai

Title: opencode-ai
# opencode-ai | Yarn. Important: This documentation covers Yarn 1 (Classic). For Yarn 2+ docs and migration guide, see yarnpkg.com. # Package detail. ## opencode-ai. Image 1GitHub Actions1.9m MIT 1.3.2. ### readme. https://yarn.pm/opencode-ai. # Use it. `$ yarn add opencode-ai`Image 5copy. Try in RunKit · Browse Files. # CDNs. jsDelivrcdn.jsdelivr.net/npm/opencode-ai/. bundle.runbundle.run/opencode-ai. # Popularity. Image 6Downloads last 30 days 1.9m. Image 7jsDelivr last 30 days 559. # Usage. # Tags. `snapshot-pnpm`0.0.0-snapshot-pnpm-202511302105. `snapshot-proxy`0.0.0-snapshot-proxy-202512080622. `latest-0`1.0.142. `snapshot-plz-help`0.0.0-snapshot-plz-help-202512122319. `snapshot-terminal-title-animation`0.0.0-snapshot-terminal-title-animation-202512231827. `snapshot-start-screen-tips`0.0.0-snapshot-start-screen-tips-202512231927. `snapshot-thinking-toggle-animated`0.0.0-snapshot-thinking-toggle-animated-202512291322. `snapshot-thinking-toggle`0.0.0-snapshot-thinking-toggle-202512300328. `snapshot-cleaner-view-subagents`0.0.0-snapshot-cleaner-view-subagents-202512310424. `snapshot-learn-agent`0.0.0-snapshot-learn-agent-202601011707. `snapshot-minimal-interrupt`0.0.0-snapshot-minimal-interrupt-202601051453. `snapshot-sort-diffs`0.0.0-snapshot-sort-diffs-202601052145. `latest-1`1.1.4. `snapshot-lander-tui`0.0.0-snapshot-lander-tui-202601070150. `snapshot-settings`0.0.0-snapshot-settings-202601070200. `snapshot-session-perf`0.0.0-snapshot-session-perf-202601082348. `snapshot-kmdr-debug`0.0.0-snapshot-kmdr-debug-202601122237. `snapshot-symlink-fix`0.0.0-snapshot-symlink-fix-202601130539. `snapshot-current-agent-animation-color`0.0.0-snapshot-current-agent-animation-color-202601160010. `snapshot-e2e-tests`0.0.0-snapshot-e2e-tests-202601191506. `snapshot-layout`0.0.0-snapshot-layout-202601191714. `snapshot-file-tree`0.0.0-snapshot-file-tree-202601251233. `snapshot-opentui-upgrade`0.0.0-snapshot-opentui-upgrade-202601260132. `ci`0.0.0-ci-202601291809. `snapshot-selection`0.0.0-snapshot-selection-202602061133. `snapshot-virtualized-diffs`0.0.0-snapshot-virtualized-diffs-202602121142. `snapshot-windows-desktop`0.0.0-snapshot-windows-desktop-202602121945. `snapshot-timeline`0.0.0-snapshot-timeline-202602192036. `snapshot-node-logic`0.0.0-snapshot-node-logic-202602200818. `snapshot-node-pty`0.0.0-snapshot-node-pty-202602201113. `snapshot-spawn-server-logs`0.0.0-snapshot-spawn-server-logs-202603052132. `beta`0.0.0-beta-202603231159. `snapshot-tui-plugins`0.0.0-snapshot-tui-plugins-202603250046. `dev`0.0.0-dev-202603250027. # Versions. March 25, 2026 0.0.0-snapshot-tui-plugins-202603250046. March 25, 2026 0.0.0-dev-202603250027. March 25, 2026 0.0.0-snapshot-tui-plugins-202603242342. # Contributors. YarnDistributed under BSD LicenseCode of Conduct.

### [3] Is it okey to delete snapshot folder? · Issue #11890 · anomalyco/opencode · GitHub

- **URL**: https://github.com/anomalyco/opencode/issues/11890

Title: Is it okey to delete snapshot folder? · Issue #11890 · anomalyco/opencode · GitHub
## Navigation Menu. # Search code, repositories, users, issues, pull requests... You signed in with another tab or window. Reload to refresh your session. You signed out in another tab or window. Reload to refresh your session. You switched accounts on another tab or window. Reload to refresh your session. * Notifications  You must be signed in to change notification settings. # Is it okey to delete snapshot folder? Is it okey to delete snapshot folder?#11890. perfIndicates a performance issue or need for optimizationIndicates a performance issue or need for optimization. ## Description. opened on Feb 3, 2026. ### Question. I'm using opencode inside ubuntu virtual machine with 80GB disk size. However, snapshot folder is so big that I cannot do anything. is it okey to delete snapshot directory? I cannot do anything because only 100MB is left in my vm. ## Metadata. ## Metadata. ### Assignees. ### Labels. perfIndicates a performance issue or need for optimizationIndicates a performance issue or need for optimization. No branches or pull requests. ## Issue actions. You can’t perform that action at this time.

### [4] opencode-folder-creator | Skills Mar... · LobeHub

- **URL**: https://lobehub.com/skills/igorwarzocha-opencode-agent-swarm-demo-opencode-folder-creator

opencode-folder-creator | Skills Mar... # opencode-folder-creator. OpenCode Folder Creator scaffolds complete .opencode directory trees for new projects, producing standard opencode.json files, agent and command subfolders, and documentation stubs so teams can run opencode serve immediately. It generates swarm-ready folder layouts including folder-specific opencode.json, AGENTS.md, primary agent markdowns, and a templated inter-agent communication protocol ("I am the [Agent\_Name] agent..."). Core advantages are speed, consistency, and agent-agnostic flexibility: the skill only creates folders and structure, so teams can plug any agent types and customize behavior afterward. Creates complete `.opencode` directory structures for projects with all necessary configuration files, agents, and commands. This skill sets up the entire OpenCode project structure that teams can immediately use with `opencode serve`. This skill is universal and works with ANY agent configuration. folder-name/ ├── AGENTS.md # Agent directory and communication protocol └── .opencode/ ├── opencode.json # Folder-specific configuration ├── agent/ │ └── folder_name.md # Primary agent with communication requirements └── command/.

### [5] opencode-folder-creator - Agent Skills

- **URL**: https://claude-plugins.dev/skills/@IgorWarzocha/opencode-agent-swarm-demo/opencode-folder-creator

Title: opencode-folder-creator - Agent Skills
@IgorWarzocha/opencode-agent-swarm-demo. Creates complete OpenCode folder structures with configuration files, agents, and commands. | description | Creates complete OpenCode folder structures with configuration files, agents, and commands |. | tags | opencode, folder, structure, configuration, setup |. Creates complete `.opencode` directory structures for projects with all necessary configuration files, agents, and commands. This skill sets up the entire OpenCode project structure that teams can immediately use with `opencode serve`. **IMPORTANT UNIVERSAL PRINCIPLE**: This skill creates FOLDERS and STRUCTURES only. The specific agent types mentioned (code-analyzer, documentation-writer, etc.) are EXAMPLES ONLY. Real swarms will have vastly different agent types, purposes, and specializations. This skill is universal and works with ANY agent configuration. # Create basic OpenCode structure mkdir -p .opencode/{agent,command} # Create main config file cat > .opencode/opencode.json << 'EOF' { "$schema": "https://opencode.ai/config.json", "theme": "opencode", "autoupdate": true } EOF. ### Specialized Agent Folder Creation (Swarm-Ready). Creates specialized agent folders with proper communication protocols for swarm deployment:. folder-name/ ├── AGENTS.md # Agent directory and communication protocol └── .opencode/ ├── opencode.json # Folder-specific configuration ├── agent/ │ └── folder_name.md # Primary agent with communication requirements └── command/. **Communication Protocol:** Every agent MUST introduce itself:. I am the [Agent_Name] agent from the [folder_name] folder. **⚠️ EXAMPLE AGENT TYPES ONLY**: The following are EXAMPLES to illustrate the pattern. Real swarms can have ANY agent types:. * `api-developer/` - API development and integration. * `game-developer/` - Game logic and mechanics. **UNIVERSAL TRUTH**: This skill works with ANY folder name, ANY agent purpose, ANY specialization. mkdir -p .opencode/{agent,command} # Code reviewer agent cat > .opencode/agent/code-reviewer.md << 'EOF' --- description: Reviews code for best practices and potential issues mode: subagent tools: read: true grep: true permissions: edit: deny write: deny --- You are a code reviewer. EOF # Main configuration cat > .opencode/opencode.json << 'EOF' { "$schema": "https://opencode.ai/config.json", "theme": "opencode", "autoupdate": true } EOF. * Use kebab-case for agent and command names. * Group related agents and commands together. * Restrict write/edit access for security-focused agents. ### Agent Design. * Create focused agents with specific purposes. This skill provides everything needed to create complete, functional OpenCode folder structures for any project type, with ready-to-use agents, commands, and configurations.

## Exa

### [1] Undo

- **URL**: https://v2.opencode.ai/snapshots

Snapshot objects can contain complete contents of tracked and non-ignored untracked files. They are stored locally in the OpenCode data directory;

### [2] 10.1 Git Snapshot System | OpenCode-Book

- **URL**: https://www.opencodebook.xyz/en/chapter_10_snapshot_and_file_system/10.1_git_snapshot_system

When an AI Agent modifies files, it can make mistakes -- it might erroneously delete code, overwrite important content, or introduce syntax errors. Without a rollback mechanism, users would have to manually restore these changes, or might not be able to restore them at all. OpenCode's Snapshot system was designed to solve exactly this problem -- it automatically creates a file system snapshot before each step of an Agent's operation, ensuring that any change can be precisely rolled back.
...
The core design decision of the Snapshot system is to use a separate, hidden Git repository to store snapshots, rather than using the project's own Git repository. This repository is located in OpenCode's global data directory:
...
```typescript
// snapshot/index.ts
function gitdir() {
  const project = Instance.project
  return path.join(Global.Path.data, "snapshot", project.id)
  // Typical path: ~/.local/share/opencode/snapshot/<project-id>
}
...
Git's `--git-dir` and `--work-tree` parameters make this "separate repository managing an external working tree" approach possible -- the Git repository metadata (`.git`) is stored in OpenCode's data directory, but the tracked files are in the project's working tree.
...
(plumbing
...
Unlike `git
...
> Section Summary
> 
> OpenCode's Snapshot system uses a separate Git repository (stored in `~/.local/share/opencode/snapshot/`) to track changes in project file states. The core operation `track()` uses `git write-tree` to create lightweight tree objects as snapshots (without creating commits), while `restore()` and `revert()` support full restoration and fine-grained file-level rollback respectively. An automatic cleanup task runs every hour, using `git gc --prune=7.days` to clean up expired data. This design ensures zero interference (no pollution of the project's Git history) while achieving fine-grained file change tracking and rollback capabilities with minimal overhead.

### [3] packages/opencode/src/snapshot/index.ts at b2baddcd · anomalyco/opencode

- **URL**: https://github.com/sst/opencode/blob/b2baddcd/packages/opencode/src/snapshot/index.ts

effect(
...
Effect.gen(function* ()
...
const fs = yield*
...
FileSystem.Service
    const sp
...
= yield*
...
ProcessSpawner
    const
...
= yield* Config
...
tree.text.trim()) {
                  log
...
op.file,
...
hash })
...
log
...
})
                yield* remove(op.file)
              })

              const clash
...
const
...
file}`])
...
return
...
const
...
const
...
const
...
const
...
",
                      )
                    }

                    const
...
}),
      restore:
...
string) {
        return
...
.diff(hash))
      }),
      diffFull
...
.diffFull")(function
...
(from: string, to: string)

### [4] phishy/opencode-snapshots

- **URL**: https://github.com/phishy/opencode-snapshots
- **Date**: 2026-01-03T19:25:03.000Z

Browse complete project state
...
any point in time
...
**Session Changes
...
from each coding session
...
thousands of snapshots captured during
...
recover lost work
...
OpenCode captures a snapshot before every AI step (tool call), storing them as git tree objects. This tool exposes that hidden history, letting you:
...
- Recover a project
...
in version control
...
Browse file contents from any point in your AI coding sessions
...
Export complete project state from any moment in time
...
OpenCode stores data in `~/.local/share/opencode/`:
...
```
~/.local/share/opencode/
├── storage/
│   ├── project/          # Project metadata
│   ├── session/          # Session metadata (titles, summaries)
│   ├── session_diff/     # Before/after file contents per session
│   └── part/             # Message parts with snapshot references
└── snapshot/             # Git object storage (trees + blobs)
    └── {projectId}/      # Bare git repo per project
```
...
### Snapshot System
...
OpenCode uses git without commits:

1. Before each AI step: `git add . && git write-tree` → captures tree hash
2. Tree hash stored in `part/{messageId}/{partId}.json`
3. All file versions preserved in `snapshot/{projectId}/objects/`

This tool scans all parts, extracts snapshot hashes, and uses `git archive` to create downloadable ZIPs.
...
| Route | Description |
| --- | --- |
| `GET /api/projects` | List all projects |
| `GET /api/projects/[id]/changes` | Get session file diffs |
| `GET /api/projects/[id]/snapshots` | Get snapshot timeline |
| `GET /api/snapshots/[projectId]/[hash]/files` | List files in snapshot |
| `GET /api/snapshots/[projectId]/[hash]/files?path=...` | Get file content |
| `GET /api/snapshots/[projectId]/[hash]/download` | Download snapshot as ZIP |
| `GET /api/sessions/[sessionId]/download` | Download session's latest snapshot as ZIP |
| `GET /api/search?q=...` | Search conversations for snapshots |

### [5] Snapshot system leaks tmp_pack_* files indefinitely, filling disk at ~9.5 GB/hour · Issue #14811 · anomalyco/opencode

- **URL**: https://github.com/anomalyco/opencode/issues/14811
- **Date**: 2026-02-23T18:30:18.000Z

The OpenCode snapshot system creates tmp_pack_* files in the git object store under ~/.local/share/opencode/snapshot/ /objects/pack/ and never cleans them up. These files accumulate at approximately one new ~9.5 GB file per hour while OpenCode is running, and persist after OpenCode exits. This will silently fill any disk to 100% capacity over time.
...
this is happening
...
> 1.Failed Snapshots: Every time you interact with the AI, OpenCode tries to snapshot your workspace. If your project is large (or if you accidentally run it in your home directory), these operations can hang.
> 
> 2.Orphaned Files: When the process hangs or is killed, it leaves those tmp_pack_... files in your .local/share/opencode/.../objects/pack folder.
> 
> 3.No Auto-Cleanup: The current version of OpenCode (as of early 2026) doesn't have a built-in mechanism to delete these temporary files automatically once they've failed.
...
> - **Project snapshot** (`0e0047...`
...
`tmp_
...
of loose objects across
...
` seems like a separate
...
tree` creates tree objects
...
stay reachable from
...
so `git gc --prune=7.days
...
533
...
>
...
> - OS
...
State dir:
...
/.local/share/op
...
> What I found locally
...
`~/.local/share
...
opencode`
...
> - one snapshot dir dominated all usage:
> 
> - `~/.local
...
share/opencode/snapshot/01adec10548001d548c
...
inside that snapshot:
> 
> -
...
pack/` =
...
39G
...
>
...
> - of that
...
`tmp_pack_*` files alone accounted for about
...
Examples from that directory:
...
> 
> - `tmp_pack_MzfB9A` =
...
5G`
> - `tmp_pack_HzAeAw` =
...
2.9G`
> - `tmp_pack_LAQ6c
...
> 
> Important detail
...
> - these files were not
...
when I inspected the snapshot dir itself
...
> This should be partially addressed by commit [0a80ef4](https://github.com/anomalyco/opencode/commit/0a80ef4278c252cb8dca72cae5d5c5748cec7e9a) which adds a 2MB file size limit for snapshotting. Large files are now excluded from snapshots, reducing tmp_pack accumulation from large files.
